class Solution
{
public:
    int makeArrayIncreasing(vector<int> &arr1, vector<int> &arr2)
    {
        int n=arr1.length();
        int dp[n+1];
        for (int i = 1; i < n; i++)
        {
            for (int j = 0; j < i; j++)
            {
               if(arr1[j]>arr1[i]){
                   dp[i]=
               }
            }
            
        }
        

    }
};